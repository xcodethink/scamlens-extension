import React from 'react';
import ReactDOM from 'react-dom/client';
import DedupePage from './DedupePage';
import '../i18n';
import '../styles/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DedupePage />
  </React.StrictMode>
);
